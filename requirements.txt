# Pin exact versions for reproducibility (SPEC §4.9)
# Run `pip-audit -r requirements.txt` to check for known CVEs
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.0
sqlalchemy==2.0.36
python-multipart==0.0.19
simpleobsws==1.4.0
slowapi==0.1.9
structlog==24.4.0
pydantic==2.10.3
pydantic-settings==2.6.1
sse-starlette==2.1.3

# Test deps (not installed in production image — see Dockerfile)
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0
